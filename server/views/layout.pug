doctype
html(lang='en')
    head
        block head
            meta(charset='utf-8')
            meta(name="viewport" content="width=device-width, initial-scale=1")
            block title
                title= title + " | Chronos"
                link(rel="shortcut-icon", href="/assets/favicon.ico")
            block styles 
                link(rel="stylesheet", href="/assets/style.css")
    body
        main
            h1.site-title= "Chronos"
            block content